spring:
  config:
    activate:
      on-profile: local
  flyway:
    enabled: ${FLYWAY_ENABLED:false}
  jpa:
    show-sql: false

logging:
  level:
    com.heapy: INFO

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
