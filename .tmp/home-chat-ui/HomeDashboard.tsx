// 작성자: 김진우 — 홈의 모든 수치와 행동은 예시이며 실제 건강기록을 조회·변경하지 않는다.
import React, { useState } from 'react';
import { BackHandler, Modal, PanResponder, Pressable, ScrollView, StyleSheet, Switch, Text, View } from 'react-native';
import LinearGradient from 'react-native-linear-gradient';
import { CompanionAvatar } from '../chat/CompanionAvatar';
import { defaultHomeSettings, HomeSettings, MetricId, metrics, ModuleId, moduleLabels, moveItem } from './homeModel';

type Props = { onConnect: () => void; onCheckup: () => void; onDetail: (id: string) => void; onChat: () => void };
const descriptions: Record<ModuleId, string> = { briefing: '오늘의 종합 분석 · 자동 구성', metrics: '수면 · 걸음 수', medication: '다음 복약 · 오늘 완료 현황', mission: '분석 기반 행동 추천', weekly: '걸음 수 · 이전 7일 비교', checkup: '최근 결과와 주의 항목' };
function Action({ title, onPress }: { title: string; onPress: () => void }) { return <Pressable accessibilityRole="button" onPress={onPress}><LinearGradient colors={['#1AB88C', '#388CF5']} start={{ x: 0, y: 0 }} end={{ x: 1, y: 0 }} style={s.action}><Text style={s.whiteBold}>{title}</Text></LinearGradient></Pressable>; }
function MetricCards({ ids }: { ids: MetricId[] }) { return <View style={s.grid}>{ids.map((id, index) => <View key={id} style={[s.metric, { backgroundColor: index % 2 ? '#EEF5FF' : '#F5F0FF', borderColor: index % 2 ? '#BBD6FF' : '#D9C7FF' }]}><Text style={s.small}>{metrics[id][0]}</Text><Text style={s.value}>{metrics[id][1]}</Text><Text style={s.purple}>{metrics[id][2]}</Text></View>)}</View>; }
function DragRow({ id, index, onMove, onRemove, onSettings }: { id: ModuleId; index: number; onMove: (from: number, to: number) => void; onRemove: () => void; onSettings: () => void }) {
  const [dy, setDy] = useState(0);
  const responder = PanResponder.create({ onStartShouldSetPanResponder: () => true, onMoveShouldSetPanResponder: () => true, onPanResponderMove: (_, g) => setDy(g.dy), onPanResponderRelease: (_, g) => { onMove(index, index + Math.round(g.dy / 66)); setDy(0); }, onPanResponderTerminate: () => setDy(0) });
  const configurable = ['metrics', 'medication', 'weekly'].includes(id);
  return <View style={[s.selectedRow, { transform: [{ translateY: dy }], zIndex: dy ? 5 : 0 }]}>
    <Pressable accessibilityLabel={`${moduleLabels[id]} ${configurable ? '설정' : '숨기기'}`} onPress={configurable ? onSettings : onRemove} style={[s.circle, !configurable && { backgroundColor: '#24B88A' }]}><Text style={s.small}>{configurable ? '⚙' : ''}</Text></Pressable>
    <Pressable accessibilityLabel={`${moduleLabels[id]} 숨기기`} onPress={onRemove} style={{ flex: 1 }}><Text style={s.rowTitle}>{moduleLabels[id]}</Text><Text style={s.caption}>{descriptions[id]}</Text></Pressable>
    <View {...responder.panHandlers} accessible accessibilityRole="adjustable" accessibilityLabel={`${moduleLabels[id]} 순서 변경`} accessibilityActions={[{ name: 'increment', label: '아래로' }, { name: 'decrement', label: '위로' }]} onAccessibilityAction={e => onMove(index, index + (e.nativeEvent.actionName === 'increment' ? 1 : -1))} style={s.drag}><Text style={s.small}>≡</Text></View>
  </View>;
}
export function HomeDashboard(_props: Props) {
  const [saved, setSaved] = useState(defaultHomeSettings);
  const [draft, setDraft] = useState(defaultHomeSettings);
  const [screen, setScreen] = useState<'home' | 'edit' | 'preview' | 'metrics' | 'medication' | 'weekly'>('home');
  const [detail, setDetail] = useState<string>();
  const [missionAdded, setMissionAdded] = useState(false);
  const [taken, setTaken] = useState(false);
  const [signal, setSignal] = useState(true);
  const [settingDraft, setSettingDraft] = useState(defaultHomeSettings);
  const configuring = ['metrics', 'medication', 'weekly'].includes(screen);
  const back = () => { if (detail) setDetail(undefined); else setScreen(screen === 'preview' || configuring ? 'edit' : 'home'); };
  React.useEffect(() => { const sub = BackHandler.addEventListener('hardwareBackPress', () => { if (screen === 'home' && !detail) return false; back(); return true; }); return () => sub.remove(); });
  const change = (patch: Partial<HomeSettings>) => setSettingDraft(value => ({ ...value, ...patch }));
  const renderModule = (id: ModuleId, config: HomeSettings) => {
    if (id === 'briefing') return <Pressable key={id} onPress={() => setDetail('今日')}><LinearGradient colors={['#09916E', '#14A3A3', '#3D6EE5']} start={{ x: 0, y: 0 }} end={{ x: 1, y: .6 }} style={s.briefing}><View style={{ flex: 1, gap: 6 }}><Text style={s.whiteSmall}>오늘의 AI 건강 브리핑</Text><Text style={s.briefingTitle}>오늘의 건강,{ '\n' }한눈에 확인해보세요</Text><Text style={s.briefChip}>어제보다 수면 +42분</Text></View><CompanionAvatar /></LinearGradient></Pressable>;
    if (id === 'metrics') return <View key={id} style={s.card}><View style={s.row}><Text style={s.heading}>오늘의 핵심 데이터</Text><Text style={s.link}>{config.metrics.length}개 선택</Text></View><MetricCards ids={config.metrics} /></View>;
    if (id === 'medication') return <View key={id} style={s.card}><View style={s.row}><Text style={s.heading}>오늘의 복약</Text>{config.medicationProgress && <Text style={s.purple}>{taken ? '2' : '1'} / 3 완료</Text>}</View>{(config.medicationMode === 'all' ? ['아침 · 오전 8:00', '다음 복약 · 오후 1:00', '저녁 · 오후 7:00'] : ['다음 복약 · 오후 1:00']).map((time, i) => <View key={time} style={s.medication}><View style={{ flex: 1, gap: 5 }}><Text style={s.purple}>{time}</Text>{config.medicationName && <Text style={s.rowTitle}>메트포르민 500mg · 1정</Text>}<Text style={s.small}>식후 복용 · 예시</Text></View>{config.medicationButton && <Pressable onPress={() => setTaken(!taken)} style={s.miniButton}><Text style={s.whiteSmall}>{taken ? '완료 취소' : '복용 완료'}</Text></Pressable>}</View>)}</View>;
    if (id === 'mission') return <LinearGradient key={id} colors={['#E8FCF5', '#DEF2FF']} start={{ x: 0, y: 0 }} end={{ x: 1, y: 1 }} style={[s.card, { borderColor: '#AFE5D7' }]}><View style={s.row}><Text style={s.link}>HEAPY 추천 미션</Text><Text style={s.purple}>활동 분석</Text></View><Text style={s.heading}>저녁에 20분 걷기</Text><Text style={s.small}>최근 7일 운동시간이 평소보다 18% 줄었어요.</Text><Pressable style={s.miniButton} onPress={() => setMissionAdded(!missionAdded)}><Text style={s.whiteSmall}>{missionAdded ? '예시 미션 추가됨 · 취소' : '미션 추가하기'}</Text></Pressable></LinearGradient>;
    if (id === 'weekly') return <View key={id} style={s.card}><Text style={s.heading}>주간 활동 변화</Text><Text style={s.value}>{config.weekly === 'steps' ? '평균 6,420보' : config.weekly === 'sleep' ? '평균 7시간 12분' : '평균 42분'}</Text><View style={s.bars}>{[34, 46, 40, 55, 30, 44, 20].map((height, i) => <View key={i} style={{ height, flex: 1, backgroundColor: i === 6 ? '#388CF5' : '#A0DDCC', borderRadius: 8 }} />)}</View><Text style={s.small}>최근 7일 ↔ 이전 7일 · 예시 데이터</Text></View>;
    return <Pressable key={id} style={s.card} onPress={() => setDetail('검진')}><Text style={s.heading}>최근 건강검진</Text><Text style={s.value}>2025.08.09</Text><Text style={s.small}>검진 결과와 주의 항목 확인하기 →</Text></Pressable>;
  };
  const title = screen === 'edit' ? '홈 편집' : screen === 'preview' ? '홈 미리보기' : screen === 'metrics' ? '핵심 데이터 설정' : screen === 'medication' ? '복약 항목 설정' : '주간 변화 설정';
  return <View style={s.root}>
    {screen !== 'home' && <View style={s.header}><Pressable accessibilityLabel="뒤로" onPress={back} style={s.touch}><Text style={s.back}>‹</Text></Pressable><Text style={s.heading}>{title}</Text><Pressable style={s.touch} onPress={() => screen === 'edit' && setDraft(defaultHomeSettings())}><Text style={s.link}>{screen === 'edit' ? '초기화' : ''}</Text></Pressable></View>}
    <ScrollView contentContainerStyle={s.page}>
      {(screen === 'home' || screen === 'preview') && <>
        <View style={s.row}><Text style={s.rowTitle}>♥ 오늘의 건강</Text>{screen === 'home' && <Pressable style={s.editButton} onPress={() => { setDraft({ ...saved, modules: [...saved.modules], metrics: [...saved.metrics] }); setScreen('edit'); }}><Text style={s.link}>홈 편집</Text></Pressable>}</View>
        <Text style={s.title}>왕밤빵님, 오늘도{ '\n' }함께 관리해요</Text><Text style={s.caption}>예시 화면 · 실제 건강 데이터가 아닙니다</Text>
        {(screen === 'home' ? saved : draft).modules.map(id => renderModule(id, screen === 'home' ? saved : draft))}
        {signal && <View style={[s.card, s.row]}><View style={{ flex: 1, gap: 6 }}><Text style={s.rowTitle}>확인할 건강 신호 1개</Text><Text style={s.caption}>혈압 기록이 3일 비었어요 · 예시</Text></View><Pressable onPress={() => setSignal(false)} style={s.editButton}><Text style={s.link}>확인</Text></Pressable></View>}
        {!(screen === 'home' ? saved : draft).modules.length && <Text style={s.small}>홈 편집에서 표시할 항목을 추가해 주세요.</Text>}
      </>}
      {screen === 'edit' && <>
        <View style={s.info}><Text style={s.rowTitle}>항목을 고르고 순서와 내용을 편집해요</Text><Text style={s.caption}>손잡이를 드래그해 순서를 바꾸고, 항목 이름을 눌러 숨겨요.</Text></View>
        <View style={s.row}><Text style={s.rowTitle}>선택된 항목 {draft.modules.length}</Text><Text style={s.caption}>≡ 순서 변경</Text></View>
        <View style={{ gap: 8 }}>{draft.modules.map((id, index) => <DragRow key={id} id={id} index={index} onMove={(from, to) => setDraft(v => ({ ...v, modules: moveItem(v.modules, from, to) }))} onRemove={() => setDraft(v => ({ ...v, modules: v.modules.filter(value => value !== id) }))} onSettings={() => { setSettingDraft({ ...draft, metrics: [...draft.metrics] }); setScreen(id as 'metrics' | 'medication' | 'weekly'); }} />)}</View>
        <Text style={[s.rowTitle, { marginTop: 16 }]}>추가할 수 있는 항목</Text>
        {(Object.keys(moduleLabels) as ModuleId[]).filter(id => !draft.modules.includes(id)).map(id => <Pressable key={id} accessibilityLabel={`${moduleLabels[id]} 추가`} style={s.unselectedRow} onPress={() => setDraft(v => ({ ...v, modules: [...v.modules, id] }))}><Text style={s.back}>＋</Text><View style={{ flex: 1 }}><Text style={s.rowTitle}>{moduleLabels[id]}</Text><Text style={s.caption}>{descriptions[id]}</Text></View><Text style={s.small}>추가</Text></Pressable>)}
      </>}
      {screen === 'metrics' && <>
        {renderModule('metrics', settingDraft)}<Text style={s.rowTitle}>표시할 항목 · {settingDraft.metrics.length} / 2 선택</Text><Text style={s.caption}>현재는 모든 항목을 예시 값으로 미리 볼 수 있어요.</Text>
        <View style={s.grid}>{(Object.keys(metrics) as MetricId[]).map(id => <Pressable key={id} onPress={() => change({ metrics: settingDraft.metrics.includes(id) ? settingDraft.metrics.filter(x => x !== id) : settingDraft.metrics.length < 2 ? [...settingDraft.metrics, id] : settingDraft.metrics })} style={[s.option, settingDraft.metrics.includes(id) && s.selected]}><Text style={s.rowTitle}>{metrics[id][0]} {settingDraft.metrics.includes(id) ? '✓' : ''}</Text><Text style={s.caption}>{metrics[id][1]}</Text></Pressable>)}</View>
        <Text style={s.rowTitle}>카드 순서</Text><Pressable style={s.info} onPress={() => change({ metrics: [...settingDraft.metrics].reverse() })}><Text style={s.small}>{settingDraft.metrics.map(id => metrics[id][0]).join('  ≡  ')} · 눌러 순서 바꾸기</Text></Pressable>
      </>}
      {screen === 'medication' && <>
        {renderModule('medication', settingDraft)}<Text style={s.rowTitle}>첫 화면에 무엇을 먼저 보여줄까요?</Text><View style={s.grid}>{(['next', 'all'] as const).map(mode => <Pressable key={mode} style={[s.option, settingDraft.medicationMode === mode && s.selected]} onPress={() => change({ medicationMode: mode })}><Text style={s.rowTitle}>{mode === 'next' ? '다음 복약 우선' : '오늘 전체 일정'}</Text><Text style={s.caption}>{mode === 'next' ? '가장 가까운 일정' : '완료 현황 중심'}</Text></Pressable>)}</View>
        <Text style={s.rowTitle}>표시 항목</Text>{(['medicationName', 'medicationButton', 'medicationProgress'] as const).map((key, i) => <View key={key} style={[s.card, s.row]}><Text style={s.rowTitle}>{['약 이름과 복용량', '복용 완료 버튼', '오늘의 완료 현황'][i]}</Text><Switch accessibilityLabel={['약 이름과 복용량', '복용 완료 버튼', '오늘의 완료 현황'][i]} value={settingDraft[key]} onValueChange={v => change({ [key]: v })} trackColor={{ true: '#747BFF', false: '#DDE5E1' }} /></View>)}
        <View style={s.info}><Text style={s.caption}>예시 복약입니다. 실제 복약 일정과 완료 기록은 변경되지 않아요.</Text></View>
      </>}
      {screen === 'weekly' && <>{renderModule('weekly', settingDraft)}<Text style={s.rowTitle}>대표 지표를 선택해 주세요</Text>{(['steps', 'sleep', 'exercise'] as const).map(id => <Pressable key={id} style={[s.option, settingDraft.weekly === id && s.selected]} onPress={() => change({ weekly: id })}><Text style={s.rowTitle}>{metrics[id][0]} {settingDraft.weekly === id ? '✓' : ''}</Text><Text style={s.caption}>최근 7일 평균과 이전 7일 비교</Text></Pressable>)}<View style={s.info}><Text style={s.small}>비교 기준 · 최근 7일 ↔ 이전 7일</Text></View></>}
    </ScrollView>
    {screen !== 'home' && <View style={s.footer}>{screen === 'preview' ? <View style={s.row}><Pressable onPress={() => setScreen('edit')} style={s.editButton}><Text style={s.small}>다시 수정</Text></Pressable><View style={{ flex: 1 }}><Action title="이대로 저장" onPress={() => { setSaved(draft); setScreen('home'); }} /></View></View> : <Action title={screen === 'edit' ? '미리보기' : '저장하고 홈 편집으로'} onPress={() => { if (screen === 'edit') setScreen('preview'); else if (screen !== 'metrics' || settingDraft.metrics.length === 2) { setDraft(settingDraft); setScreen('edit'); } else setDetail('핵심 데이터는 2개를 선택해 주세요.'); }} />}</View>}
    <Modal visible={!!detail} transparent animationType="fade" onRequestClose={() => setDetail(undefined)}><View style={s.overlay}><View style={s.card}><Text style={s.heading}>{detail === '今日' ? '오늘의 AI 건강 브리핑' : detail === '검진' ? '최근 건강검진' : detail}</Text><Text style={s.small}>{detail === '今日' ? '수면은 회복 중이고 활동량은 감소했어요.\n수면 7시간 12분 · 걸음 5,920보' : '홈 UI 미리보기용 예시 화면입니다.'}</Text><Action title="확인" onPress={() => setDetail(undefined)} /></View></View></Modal>
  </View>;
}
const s = StyleSheet.create({ root: { flex: 1 }, page: { padding: 20, gap: 14, paddingBottom: 28 }, header: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', paddingHorizontal: 16, minHeight: 60 }, touch: { minWidth: 48, minHeight: 44, justifyContent: 'center' }, back: { fontSize: 27, color: '#143B30' }, title: { fontSize: 26, lineHeight: 32, fontWeight: '800', color: '#17342D' }, row: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', gap: 10 }, heading: { fontSize: 16, fontWeight: '800', color: '#17342D' }, rowTitle: { fontSize: 13, fontWeight: '700', color: '#143B30' }, small: { fontSize: 12, lineHeight: 18, color: '#617A70' }, caption: { fontSize: 10, lineHeight: 16, color: '#617A70' }, link: { fontSize: 11, fontWeight: '600', color: '#1AAD80' }, purple: { fontSize: 11, color: '#7370ED' }, whiteSmall: { fontSize: 11, color: 'white', lineHeight: 16 }, whiteBold: { fontSize: 14, fontWeight: '700', color: 'white' }, card: { padding: 16, gap: 12, borderRadius: 22, borderWidth: 1, borderColor: '#DBEBE5', backgroundColor: '#FFFFFF', boxShadow: '0px 5px 16px rgba(9,41,32,0.07)' }, briefing: { padding: 16, borderRadius: 24, flexDirection: 'row', alignItems: 'center', minHeight: 124, gap: 8, boxShadow: '0px 8px 22px rgba(9,41,32,0.12)' }, briefingTitle: { fontSize: 18, fontWeight: '700', lineHeight: 23, color: 'white' }, briefChip: { color: 'white', fontSize: 10, backgroundColor: '#FFFFFF30', padding: 4, borderRadius: 10, alignSelf: 'flex-start' }, grid: { flexDirection: 'row', flexWrap: 'wrap', gap: 10 }, metric: { flex: 1, padding: 12, borderWidth: 1, borderRadius: 16, gap: 7, minWidth: 120 }, value: { fontSize: 22, fontWeight: '800', color: '#173A31' }, medication: { backgroundColor: '#EEF5FF', padding: 12, borderRadius: 18, flexDirection: 'row', alignItems: 'center', gap: 8 }, miniButton: { backgroundColor: '#24B889', borderRadius: 15, minHeight: 44, padding: 12, alignItems: 'center', justifyContent: 'center' }, bars: { height: 62, flexDirection: 'row', gap: 12, alignItems: 'flex-end' }, editButton: { padding: 12, borderRadius: 20, backgroundColor: '#FFFFFFCC', borderWidth: 1, borderColor: '#DBEBE5', minHeight: 44 }, info: { padding: 14, borderRadius: 18, backgroundColor: '#E8FAF2', gap: 8 }, selectedRow: { height: 58, borderRadius: 16, borderWidth: 1.5, borderColor: '#24B88A', flexDirection: 'row', alignItems: 'center', backgroundColor: 'white', paddingLeft: 10, gap: 20 }, circle: { height: 28, width: 28, borderRadius: 14, backgroundColor: '#E8EDEB', alignItems: 'center', justifyContent: 'center' }, drag: { width: 48, height: 56, alignItems: 'center', justifyContent: 'center' }, unselectedRow: { minHeight: 58, padding: 12, flexDirection: 'row', alignItems: 'center', gap: 14, borderRadius: 16, borderWidth: 1, borderColor: '#D4DED9', backgroundColor: '#EBF0ED' }, option: { flexGrow: 1, flexBasis: '45%', borderRadius: 18, padding: 14, minHeight: 64, gap: 5, borderWidth: 1, borderColor: '#DBEBE5', backgroundColor: 'white' }, selected: { backgroundColor: '#E6FAF3', borderColor: '#24B88A' }, footer: { padding: 20, backgroundColor: '#F6FBF9' }, action: { minHeight: 52, alignItems: 'center', justifyContent: 'center', borderRadius: 16 }, overlay: { flex: 1, justifyContent: 'center', padding: 24, backgroundColor: '#122D2570' } });
